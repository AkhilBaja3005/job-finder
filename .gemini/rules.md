# Git Rules
- NEVER run `git push` under any circumstances (including pushing to origin, upstream, hf, or any other remote).
- You may run `git status`, `git diff`, `git add`, and `git commit` if needed, but always leave `git push` to the user.
